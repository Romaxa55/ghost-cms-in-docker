apiVersion: v1
kind: Service
metadata:
  name: {{ include "ghost-cms.fullname" . }}
  labels:
    {{- include "ghost-cms.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ghost-cms.selectorLabels" . | nindent 4 }}
