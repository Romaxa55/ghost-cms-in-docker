apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ghost-cms.fullname" . }}-test-connection"
  labels:
    {{- include "ghost-cms.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ghost-cms.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
